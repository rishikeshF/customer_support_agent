# Core database

`udahub.db` goes here. Built by `02_core_db_setup.ipynb`.

Tables: `accounts`, `users`, `tickets`, `ticket_metadata`, `ticket_messages`,
`knowledge`.

`customer_memory` is also here. It holds long-term customer preferences and is
created automatically the first time the agent runs, so the setup notebook does
not need to know about it.
